@model InjectCC.Model.Location

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Location</legend>

    <div class="display-label">Name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Name)
    </div>

    <div class="display-label">Ordinal</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Ordinal)
    </div>

    <div class="display-label">TimeUntilNextInjection</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TimeUntilNextInjection)
    </div>

    <div class="display-label">LocationSet</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.LocationSet.Name)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
